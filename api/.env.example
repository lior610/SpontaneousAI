# Local PostgreSQL (use these to point API at your local DB)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
# Homebrew on macOS: leave unset to use your macOS username (no password)
# Or set both: POSTGRES_USER=postgres POSTGRES_PASSWORD=postgres
# POSTGRES_USER=
# POSTGRES_PASSWORD=
POSTGRES_USERS_DB=users
